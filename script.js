// Setting variables ...
const mobileMenu = document.getElementById("mobile-menu");

// The function to close the mobile navigation menu
const openMobileMenu = () => {
    mobileMenu.style.width = "100vw";
}

// The function to close the mobile navigation menu
const closeMobileMenu = () => {
    mobileMenu.style.width = "0";
}